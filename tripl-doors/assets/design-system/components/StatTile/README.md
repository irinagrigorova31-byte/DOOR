The proof statistic — a large azure figure over a quiet uppercase caption, as in the "120+ / ТЫСЯЧ ИЗДЕЛИЙ" row. Statistics are the brand's loudest voice; lay several in a single horizontal band on `surface-muted`.

Consumer provides `value` (the headline figure, kept short — "120+", "99.9"), an optional `unit` rendered smaller inside the figure ("минут", "мм", "%"), and `label` for the caption. The figure uses the `display` scale in `blue`; the caption uses `label` in `text-muted`.

The figure clears contrast because it is 56px (the 3:1 large-text floor). The caption is small `text-muted` and sits below 4.5:1 — acceptable here because it is a secondary label paired with the figure, not standalone reading. Keep captions to two or three words.
