The uppercase azure section title that opens each full-width block ("ПОЧЕМУ НАС ВЫБИРАЮТ"). It sets the `h2` style — 22px bold, `0.06em` tracking, uppercased — in `blue`.

Consumer provides the text as children and may override the tag with `as` (`h1`…`h3`) to keep the document outline correct while keeping the look. At 22px bold the `blue` clears the 3:1 large-text floor on both `white` and `surface-muted`.

Use one per section, left-aligned, with `space-8` between it and the content below. Keep the text a few words; the component uppercases via CSS, so pass normal-case Russian and let it transform.
